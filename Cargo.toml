[package]
name = "meka"
version = "0.52.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"
description = "A general-purpose AI agent harness"
repository = "https://github.com/k4yt3x/meka"

[features]
default = ["serve"]
mock-provider = []
serve = ["dep:axum", "dep:tower-http", "dep:utoipa", "dep:utoipa-swagger-ui"]

[dependencies]
agent-client-protocol = { version = "2.0", features = ["unstable_elicitation", "unstable_end_turn_token_usage", "unstable_session_fork"] }
anyhow = "1"
async-stream = "0.3"
async-trait = "0.1"
axum = { version = "0.8", optional = true }
base64 = "0.23"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive", "wrap_help"] }
croner = "3"
crossterm = "0.29"
dirs = "6"
eventsource-stream = "0.2"
fast_html2md = "0.0"
futures = "0.3"
glob = "0.3"
grep-matcher = "0.1"
grep-regex = "0.1"
grep-searcher = "0.1"
hmac = "0.13.0"
humantime-serde = "1"
image = { version = "0.25", default-features = false, features = ["bmp", "dds", "exr", "ff", "gif", "hdr", "ico", "jpeg", "png", "pnm", "qoi", "tga", "tiff", "webp"] }
infer = { version = "0.22", default-features = false }
nu-ansi-term = "0.50"
open = "5"
percent-encoding = "2"
pulldown-cmark = { version = "0.13.4", default-features = false }
rand = "0.10"
reedline = { version = "0.50", features = ["external_printer"] }
regex = "1"
reqwest = { version = "0.13", features = ["json", "query", "stream", "gzip", "deflate", "brotli", "cookies"] }
rmcp = { version = "3.1", features = ["client", "transport-child-process", "transport-streamable-http-client-reqwest", "auth", "auth-client-credentials-jwt"] }
rusqlite = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
serde_norway = "0.9.42"
sha2 = "0.11"
subtle = "2"
syntect = { version = "5", default-features = false, features = ["default-syntaxes", "parsing", "plist-load", "regex-fancy"] }
termimad = "0.35"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tokio-rusqlite = "0.7"
tokio-util = { version = "0.7", features = ["compat"] }
toml = "1.1"
toml_edit = "0.25"
tower-http = { version = "0.7", features = ["limit", "trace"], optional = true }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
unicode-width = "0.2"
url = "2"
utoipa = { version = "5", features = ["axum_extras", "chrono", "uuid"], optional = true }
utoipa-swagger-ui = { version = "9", features = ["axum", "vendored"], optional = true }
uuid = { version = "1", features = ["v4", "serde"] }
zune-core = "0.5"
zune-jpeg = "0.5"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_Security", "Win32_Security_Authorization", "Win32_Storage_FileSystem", "Win32_System_Console", "Win32_System_JobObjects", "Win32_System_Pipes", "Win32_System_Threading", "Win32_UI_WindowsAndMessaging"] }

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"

[dev-dependencies]
filetime = "0.2.29"
http = "1"
reqwest = { version = "0.13", features = ["blocking", "json"] }
tempfile = "3"
tokio = { version = "1", features = ["test-util"] }

[target.'cfg(unix)'.dev-dependencies]
libc = "0.2"

[target.'cfg(windows)'.dev-dependencies]
windows-sys = { version = "0.61", features = ["Win32_System_Diagnostics_Debug"] }

[lints.clippy]
await_holding_lock = "deny"
expect_used = "warn"
indexing_slicing = "allow"
match_wildcard_for_single_variants = "warn"
panic = "warn"
redundant_clone = "warn"
result_large_err = "allow"
significant_drop_tightening = "warn"
uninlined_format_args = "warn"
unreachable = "warn"
unused_async = "warn"
unwrap_used = "warn"

[profile.release]
strip = "symbols"
lto = "fat"
codegen-units = 1
