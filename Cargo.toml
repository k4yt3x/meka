[package]
name = "agsh"
version = "0.5.0"
edition = "2024"
license = "MIT"
description = "An agentic shell where you speak human, not bash"
repository = "https://github.com/k4yt3x/agsh"

[[bin]]
name = "agsh"
path = "src/main.rs"

[dependencies]
anyhow = "1"
async-trait = "0.1"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
dirs = "6"
eventsource-stream = "0.2"
fast_html2md = { version = "0.0", features = ["rewriter"] }
futures = "0.3"
glob = "0.3"
grep-matcher = "0.1"
grep-regex = "0.1"
grep-searcher = "0.1"
open = "5"
rand = "0.9"
reedline = "0.38"
reqwest = { version = "0.13", default-features = false, features = ["json", "query", "stream", "rustls", "gzip", "deflate", "brotli"] }
rmcp = { version = "1.2", features = ["client", "transport-child-process", "transport-streamable-http-client-reqwest", "auth", "auth-client-credentials-jwt"] }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
termimad = "0.30"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tokio-rusqlite = "0.6"
tokio-util = "0.7"
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1", features = ["v4", "serde"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"

[dev-dependencies]
tempfile = "3"

[profile.release]
strip = "symbols"
lto = "fat"
codegen-units = 1
